export enum EAppInputHeight {
  'DEFAULT' = 'DEFAULT',
  'MEDIUM' = 'MEDIUM',
  'LARGE' = 'LARGE',
}

export enum EAppInputBorderWidth {
  'MEDIUM' = 2,
  'LARGE' = 3,
}

export enum EAppInputBackground {
  'SEARCH' = 'SEARCH',
}
